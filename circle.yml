machine:
  timezone:
    America/Toronto
  node:
    version: 6.10.0

compile:
  override:
    - npm install -g gulp
    - gulp

deployment:
  staging:
    branch: master
    commands:
      - aws s3 sync ./ s3://no-limit-home-repair --delete
